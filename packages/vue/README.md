# eslint-config-rchl-vue

## Installation

```sh
npm i -D eslint-config-rchl-vue eslint
```

## Usage

Add in `package.json`:

```json
  "eslintConfig": {
    "extends": [
      "eslint-config-rchl-vue"
    ]
  },
```
